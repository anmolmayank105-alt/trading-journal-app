# Environment Configuration
NODE_ENV=development

# Database Configuration
# Local MongoDB (for development)
MONGODB_URI_LOCAL=mongodb://admin:admin123@localhost:27017/stock_tracker?authSource=admin

# MongoDB Atlas (production)
MONGODB_URI=mongodb+srv://starunkumarainds2024_db_user:2fgmUJliWHq9YUIl@cluster0.bc9ss4x.mongodb.net/
MONGODB_DB_NAME=trading_analytics

# Redis Configuration (local development)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Redis (production - AWS ElastiCache)
REDIS_URL=redis://your-elasticache-endpoint:6379
REDIS_TLS_ENABLED=true

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-this-in-production
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# API Configuration
API_PORT=3000
API_BASE_URL=http://localhost:3000

# Zerodha Configuration
ZERODHA_API_KEY=your-zerodha-api-key
ZERODHA_API_SECRET=your-zerodha-api-secret
ZERODHA_REDIRECT_URL=http://localhost:3000/callback/zerodha

# Upstox Configuration
UPSTOX_API_KEY=your-upstox-api-key
UPSTOX_API_SECRET=your-upstox-api-secret
UPSTOX_REDIRECT_URL=http://localhost:3000/callback/upstox

# Market Data API
MARKET_DATA_PROVIDER=finnhub
MARKET_DATA_API_KEY=your-market-data-api-key
MARKET_DATA_BASE_URL=https://finnhub.io/api/v1

# AWS Configuration (for production deployment)
AWS_REGION=ap-south-1
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_S3_BUCKET=stock-tracker-reports
AWS_LAMBDA_ROLE_ARN=arn:aws:iam::account-id:role/lambda-execution-role

# Email Configuration (AWS SES)
EMAIL_FROM=noreply@stocktracker.com
AWS_SES_REGION=ap-south-1

# SMS Configuration (AWS SNS)
AWS_SNS_REGION=ap-south-1

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=debug

# CORS
CORS_ORIGIN=http://localhost:3001

# Broker Sync Schedule (cron format)
BROKER_SYNC_SCHEDULE=*/5 * * * *

# Cache TTL (in seconds)
CACHE_TTL_MARKET_DATA=5
CACHE_TTL_ANALYTICS=900
CACHE_TTL_USER_SESSION=3600
